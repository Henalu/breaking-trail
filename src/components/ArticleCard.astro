---
interface Props {
  slug: string
  title: string
  description: string
  categoria: string
  dificultad: string
  fecha: Date
  servicenow_version: string[]
  resuelto: boolean
}

const { slug, title, description, categoria, dificultad, fecha, servicenow_version, resuelto } = Astro.props

const categoriaLabel: Record<string, string> = {
  'awa': 'Advanced Work Assignment',
  'cmdb': 'CMDB',
  'assignment-engine': 'Assignment Engine',
  'integraciones': 'Integraciones',
  'business-rules': 'Business Rules',
  'transform-maps': 'Transform Maps',
  'ui-actions': 'UI Actions',
}

const dificultadLabel: Record<string, string> = {
  basico: 'Básico',
  intermedio: 'Intermedio',
  avanzado: 'Avanzado',
}

const dificultadColor: Record<string, string> = {
  basico: 'text-green-400',
  intermedio: 'text-yellow-400',
  avanzado: 'text-red-400',
}
---

<a
  href={`/articulos/${slug}`}
  class="block p-5 rounded-lg border border-gray-800 hover:border-gray-600 hover:bg-gray-900/50 transition-colors group"
>
  <div class="flex items-center gap-2 mb-2">
    <span class="text-xs text-gray-500">{categoriaLabel[categoria] ?? categoria}</span>
    <span class="text-gray-700">·</span>
    <span class={`text-xs font-medium ${dificultadColor[dificultad]}`}>
      {dificultadLabel[dificultad]}
    </span>
    {!resuelto && (
      <span class="text-xs text-orange-400">· Sin solución completa</span>
    )}
  </div>
  <h2 class="text-lg font-semibold text-white group-hover:text-blue-400 transition-colors mb-1">
    {title}
  </h2>
  <p class="text-gray-400 text-sm line-clamp-2">{description}</p>
  <p class="text-gray-600 text-xs mt-3">
    {fecha.toLocaleDateString('es-ES', { year: 'numeric', month: 'long', day: 'numeric' })}
    · {servicenow_version.join(', ')}
  </p>
</a>
